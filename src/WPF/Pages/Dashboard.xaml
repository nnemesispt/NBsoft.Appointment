<UserControl x:Class="NBsoft.Appointment.WPF.Pages.Dashboard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NBsoft.Appointment.WPF.Pages"
             xmlns:mui="http://firstfloorsoftware.com/ModernUI"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Style="{StaticResource ContentRoot}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="100*" />
        </Grid.RowDefinitions>
        <TextBlock Text="{DynamicResource app.dashboard.header}" Style="{StaticResource Heading2}" />
        <Grid Grid.Row="1" Margin="10">
            <mui:ModernFrame Name="FrameDetails" Source="Content/DashboardStart.xaml" />
        </Grid>
    </Grid>
</UserControl>
